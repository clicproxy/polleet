{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Answer list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Questionnaire</th>
                <th>Personne</th>
                <th>Réponse</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.poll }}</td>
                <td>{{ entity.person }}</td>
                <td>{{ entity.pollanswer }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('answer_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('answer_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('answer_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
